{
  "name": "flux-async-boilerplate",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^3.4.0",
    "flux": "^3.1.2",
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "xhr": "^2.4.0"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "concurrently \"json-server --watch db.json --port 3030\" \"react-scripts start\"",
    "server": "json-server --watch db.json --port 3030",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
